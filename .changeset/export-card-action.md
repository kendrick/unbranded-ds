---
'@unbranded-ds/react': minor
---

Export `CardAction` from the Card barrel. The component was already defined in `Card.tsx` but missing from `Card/index.ts`, so consumers couldn't import it. Surfaced while authoring the Card sidecar for spec 006.
