# Spec-007 inbox

Observed TSDoc/JSDoc drift collected during the sidecar-retrofit pass (spec 006). Spec 007 owns resolution. Authors append here; do not modify the source `.tsx` files in sidecar PRs.

**Status as of spec 007 completion: all bullets closed.**

---

- ~~`packages/react/src/components/Button/Button.stories.tsx:13-18` — `argTypes` for `size` lists only `['default', 'xs', 'sm', 'lg', 'icon']` but the CVA definition in `Button.tsx` includes `'icon-xs'`, `'icon-sm'`, and `'icon-lg'` as valid size values. The stories control panel silently omits those three options. Observed while authoring `Button.usage.md`.~~ — **Closed by 6a29bff (docs(react): audit Button TSDoc and stories).** The `ButtonProps` interface now declares all eight `size` values; the stale `argTypes` block in `Button.stories.tsx` was removed so react-docgen drives the Controls panel from the prop interface.

- ~~`packages/react/src/components/Dialog/Dialog.tsx:43-48` — `DialogContent`'s `showCloseButton` prop (a component-specific addition on top of `DialogPrimitive.Popup.Props`) has no JSDoc comment. Its purpose and default are clear from the implementation but not from the type alone. Observed while authoring `Dialog.usage.md`.~~ — **Closed by aa4dfd5 (docs(react): audit Dialog TSDoc and stories).** `DialogContent` now has a `DialogContentOwnProps` interface with TSDoc on `showCloseButton` covering WHAT (renders the X icon in the top-right corner) and WHEN (set to `false` when DialogFooter provides the close action so the affordances don't duplicate).

- ~~`packages/react/src/components/Dialog/Dialog.tsx:92-98` — `DialogFooter`'s `showCloseButton` prop has no JSDoc comment. The prop's relationship to `DialogContent`'s same-named prop (they serve opposite defaults and render in different positions) warrants a comment explaining when you'd use one versus the other. Observed while authoring `Dialog.usage.md`.~~ — **Closed by aa4dfd5 (docs(react): audit Dialog TSDoc and stories).** `DialogFooter` now has a `DialogFooterOwnProps` interface with TSDoc on `showCloseButton` that explicitly names the inverse relationship with `DialogContent`'s same-named prop: opposite defaults (`true` vs `false`), opposite positions (X icon top-right vs named text button at the end of the action row), and the useful pairing pattern.

- ~~`packages/react/src/components/Slider/Slider.tsx:92-106` — `SliderControlProps`, `SliderTrackProps`, `SliderIndicatorProps`, and `SliderThumbProps` interfaces have no property-level TSDoc. `SliderRootProps` properties are also undocumented at the interface level; descriptions live only in Storybook `argTypes`. Observed while authoring `Slider.usage.md`.~~ — **Closed by cd4cffb (docs(react): audit Slider TSDoc and stories).** All five Slider prop interfaces now carry per-property TSDoc. `SliderRootProps` documents the array-shape contract for `value` and `defaultValue` plus the structured-warning fallbacks for invalid `step` and out-of-bounds values; the four slot interfaces each document `className`. The duplicate `argTypes` block in `Slider.stories.tsx` was removed.

- ~~`packages/react/src/components/Slider/Slider.tsx:255-293` — `SliderControl`, `SliderTrack`, `SliderIndicator`, and `SliderThumb` function declarations carry no JSDoc comment describing their role in the slot composition contract. Observed while authoring `Slider.usage.md`.~~ — **Closed by cd4cffb (docs(react): audit Slider TSDoc and stories).** All five slot functions now carry per-slot TSDoc describing their composition role plus an `@see {@link Slider}` pointer back to the overview block on the `Slider` object literal.

- ~~`packages/react/src/components/SegmentedControl/SegmentedControl.tsx:67-83` — `SegmentedControlRootProps` and `SegmentedControlItemProps` interfaces have no property-level TSDoc comments; prop descriptions live only in Storybook `argTypes`. Observed while authoring `SegmentedControl.usage.md`.~~ — **Closed by 8d02a54 (docs(react): audit SegmentedControl TSDoc and stories).** Both interfaces now carry per-property TSDoc with the WHAT + WHEN bar plus `@defaultValue` tags where defaults exist. The duplicate `argTypes` block and the `meta.parameters.docs.description.component` value in `SegmentedControl.stories.tsx` were removed so react-docgen drives the autodoc banner and Controls from a single source.
