// Tailwind CSS v4 is a PostCSS plugin. This single plugin processes the
// `@import 'tailwindcss'` and the design-system preset import in globals.css.
const config = {
	plugins: {
		'@tailwindcss/postcss': {},
	},
};

export default config;
